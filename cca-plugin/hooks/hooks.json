{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "echo \"🤖 CCA Active\" && if [ -d .cca ]; then echo \"📚 Notes: $(ls -1 .cca/notes 2>/dev/null | grep -c '.md$') | 🔤 $(if [ -f package.json ]; then echo 'JS/TS'; elif [ -f Cargo.toml ]; then echo 'Rust'; elif [ -f go.mod ]; then echo 'Go'; elif [ -f pyproject.toml ] || [ -f requirements.txt ]; then echo 'Python'; elif ls *.csproj >/dev/null 2>&1; then echo 'C#'; else echo 'Unknown'; fi) | 🌿 $(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo 'no git')\"; else echo \"⚠️ CCA not initialized. Run /cca:init to set up.\"; fi"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/post-edit.sh"
          }
        ]
      }
    ]
  }
}
